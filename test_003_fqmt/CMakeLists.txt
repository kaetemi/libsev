
FILE(GLOB SRCS *.cpp)
FILE(GLOB HDRS *.h)
FILE(GLOB INLS *.inl)

SOURCE_GROUP("" FILES ${SRCS} ${HDRS} ${INLS})

ADD_EXECUTABLE(test_003_fqmt
  ${SRCS}
  ${HDRS}
  ${INLS}
)

TARGET_LINK_LIBRARIES(test_003_fqmt
  sev
  psapi
)

#ADD_DEFINITIONS(-DSEV_LIB_STATIC)
