
FILE(GLOB SRCS *.cpp)
FILE(GLOB HDRS *.h)
FILE(GLOB INLS *.inl

SOURCE_GROUP("" FILES ${SRCS} ${HDRS} ${INLS})

ADD_LIBRARY(sev SHARED
	${SRCS}
	${HDRS}
	${RESOURCE_ADDED}
)

TARGET_LINK_LIBRARIES(sev
)

ADD_DEFINITIONS(-DSEV_LIB_EXPORT)
